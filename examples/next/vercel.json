{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm ci --no-audit --no-fund && npm run build && cd examples/next && npm install --no-audit --no-fund",
  "buildCommand": "next build",
  "outputDirectory": ".next"
}
